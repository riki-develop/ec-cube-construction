{#
This file is part of EC-CUBE

Copyright(c) EC-CUBE CO.,LTD. All Rights Reserved.

http://www.ec-cube.co.jp/

For the full copyright and license information, please view the LICENSE
file that was distributed with this source code.
#}
{% extends 'default_frame.twig' %}

{% set body_class = 'cart_page' %}

{% block main %}
  <div class="row">
    <div class="col">
      <h1 class="h4 py-2 my-2 border-bottom">{{ 'ご注文完了'|trans }}</h1>
    </div>
  </div>

  <div class="row text-center justify-content-center text-secondary d-none d-sm-flex">
    <div class="col-auto bg-light px-4 py-2 rounded-pill mx-2">
      <i class="bi bi-check-square me-1"></i>{{ 'カートの商品'|trans }}
    </div>
    {% if is_granted('ROLE_USER') == false %}
    <div class="col-auto bg-light px-4 py-2 rounded-pill mx-2">
      <i class="bi bi-check-square me-1"></i>{{ 'お客様情報'|trans }}
    </div>
    {% endif %}
    <div class="col-auto bg-light px-4 py-2 rounded-pill mx-2">
      <i class="bi bi-check-square me-1"></i>{{ 'ご注文手続き'|trans }}
    </div>
    <div class="col-auto bg-light px-4 py-2 rounded-pill mx-2">
      <i class="bi bi-check-square me-1"></i>{{ 'ご注文内容確認'|trans }}
    </div>
    <div class="col-auto bg-light shadow-sm px-4 py-2 rounded-pill mx-2 fw-bold">
      <i class="bi bi-check-square me-1"></i>{{ '完了'|trans }}
    </div>
  </div>

  <div class="row mt-3">
    <div class="col">
      <h2>{{ 'ご注文ありがとうございました'|trans }}</h2>
      <p class="ec-reportDescription">
        ただいま、ご注文の確認メールをお送りさせていただきました。<br>
        万一、ご確認メールが届かない場合は、トラブルの可能性もありますので大変お手数ではございますがお問い合わせくださいますようお願いいたします。<br>
        {% if Order.id %}
          <br /><br /><strong>{{ 'ご注文番号'|trans }} : {{ Order.orderNo }}</strong>
        {% endif %}
      </p>

      {% if Order.complete_message is not empty %}
        {{ Order.complete_message|raw }}
      {% endif %}

      <div class="text-center">
        {% if hasNextCart %}
          <a class="btn btn-lg btn-primary" href="{{ url('cart') }}">{{ '購入を続ける'|trans }}</a>
        {% else %}
          <a class="btn btn-lg btn-primary" href="{{ url('homepage') }}">{{ 'トップページへ'|trans }}</a>
        {% endif %}
      </div>

    </div>
  </div>

{% endblock %}
